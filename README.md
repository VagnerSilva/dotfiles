# dotfiles

## Uninstall

Remove the links and files created by this setup with:

	bash uninstall.sh

Use `bash uninstall.sh --yes` for a non-interactive cleanup. System packages are
left installed; restoring the default shell is optional.